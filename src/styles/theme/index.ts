import light from './colors/light';
import dark from './colors/dark';

export default {
  color: light,
  colors: { light, dark },
  font: {
    weight: {
      regular: 400,
      semibold: 600,
      bold: 700,
    },
  },
};
